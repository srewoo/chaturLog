# ChaturLog Frontend Environment Variables
# Copy this file to .env and fill in your actual values

# ==================== Backend API Configuration ====================
# Backend API URL
# Development
REACT_APP_BACKEND_URL=http://localhost:8001
# Production example: https://api.chaturlog.yourdomain.com

# ==================== Application Configuration ====================
# Application name
REACT_APP_NAME=ChaturLog
# Application version
REACT_APP_VERSION=1.0.0

# ==================== File Upload Configuration ====================
# Maximum file size in MB
REACT_APP_MAX_FILE_SIZE_MB=50
# Allowed file types (comma-separated)
REACT_APP_ALLOWED_FILE_TYPES=.log,.txt,.json

# ==================== Feature Flags ====================
# Enable analytics (currently disabled)
REACT_APP_ENABLE_ANALYTICS=false
# Enable debug mode
REACT_APP_ENABLE_DEBUG=false

# ==================== Webpack Dev Server (Optional) ====================
# Uncomment these to fix WebSocket warnings
# WDS_SOCKET_PORT=3000
# WDS_SOCKET_HOST=localhost
# DISABLE_ESLINT_PLUGIN=true

# ==================== Build Configuration ====================
# Generate source maps in production
GENERATE_SOURCEMAP=false
# Skip preflight check
SKIP_PREFLIGHT_CHECK=true

# ==================== Notes ====================
# 1. Copy this file to .env (for all environments)
# 2. Or create .env.local (for local development - gitignored)
# 3. Or create .env.production (for production builds)
# 4. Never commit actual .env files to git
# 5. API keys are configured per-user in the Settings page, not here
# 6. Update REACT_APP_BACKEND_URL to match your backend in production
# 7. Restart the dev server after changing environment variables
